import styled from "styled-components";

export const OrderContainer = styled.main`
  margin-top: 4.4rem;
  display: flex;

  form {
    gap: 1.4rem;
    display: flex;
    flex-direction: row;
  }
  
`
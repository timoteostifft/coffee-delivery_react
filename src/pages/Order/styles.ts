import styled from "styled-components";

export const OrderContainer = styled.main`
  margin-top: 8rem;
  padding: 0 20px;
  display: flex;

  form {
    gap: 1.4rem;
    display: flex;
    flex-direction: row;
  }
  
`